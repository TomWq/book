# AI 网文写作助手内测说明

本文档用于第一批内测用户安装、激活和反馈问题。当前内测重点是验证作者端客户端的稳定性、授权激活流程、AI 设置和创作主流程。

## 内测版本

```text
产品名称：AI 网文写作助手
当前版本：1.0.0
发布平台：Windows x64
安装包：AI网文写作助手-Setup-1.0.0-x64.exe
```

当前 Windows 安装包位置：

```text
/Volumes/xinxin/book/release/AI网文写作助手-Setup-1.0.0-x64.exe
```

如果发 macOS 内测包，仅发布 Apple 芯片版本：

```text
M 系列 Mac：mac arm64 包
```

旧款 x64 Mac 暂不提供内测安装包。

## 适合测试的人

1. 正在写网文或准备开新书的作者。
2. 需要拆解爆款小说结构的拆书用户。
3. 愿意完整跑一遍“创建项目、配置 AI、生成章节、审稿修改”的内测用户。

## 运行要求

1. Windows 10 或 Windows 11，建议 64 位系统。
2. 能正常访问你的 AI 服务商接口。
3. 已准备好自己的 AI 请求地址、API Key 和模型名。
4. 已获得内测授权码。

## 安装方式

### Windows

1. 双击安装包 `AI网文写作助手-Setup-1.0.0-x64.exe`。
2. 按提示选择安装目录。
3. 安装完成后，从桌面快捷方式或开始菜单打开 `AI 网文写作助手`。

如果 Windows 提示未知发布者，这是因为当前内测包还没有做正式代码签名。确认安装包来源可信后，可以选择继续运行。

### macOS

当前 macOS 内测包会做 ad-hoc 签名，但暂未做 Apple Developer ID 公证，所以第一次打开时 macOS 可能提示“无法验证开发者”或“已损坏”。

1. 下载与你电脑芯片匹配的 zip。
2. 解压后，把 `AI 网文写作助手.app` 拖入“应用程序”。
3. 按住 Control 点击应用图标，选择“打开”，再确认打开。
4. 如果仍提示“已损坏，无法打开”，打开“终端”执行：

```bash
xattr -dr com.apple.quarantine "/Applications/AI 网文写作助手.app"
```

执行后再次打开应用。

## 首次激活

1. 第一次打开客户端时，输入内测授权码。
2. 激活成功后会进入作者端工作台。
3. 一个授权码默认只允许激活一台设备。
4. 如果更换电脑或重装系统后无法激活，请联系管理员处理。

不要把授权码公开发到群里或截图里。反馈问题时只需要说明授权码后四位即可。

## AI 设置

进入客户端后，先打开 AI 设置页，填写自己的 AI 服务信息：

```text
请求地址
API Key
模型名
```

建议先使用你平时稳定可用的模型。配置完成后，可以用页面里的测试功能确认连接是否正常。

## 推荐测试流程

### 1. 创建写作项目

1. 进入项目中心。
2. 创建一个新写作项目。
3. 填写题材、主题、角色、简介或创作方向。
4. 测试 AI 起名和简介生成是否符合预期。

重点观察：

1. 题材、主题、角色是否被 AI 生硬拼进标题。
2. 简介是否符合选择的风格。
3. 首页和项目中心的章节数、字数统计是否同步。

### 2. 生成章节任务卡

1. 进入创作工作台。
2. 生成第一章任务卡。
3. 不满意时删除后重新生成。
4. 继续生成第二章、第三章，观察上下文是否正常衔接。

重点观察：

1. 删除章节后，重新生成是否还引用已删除内容。
2. 第一章是否错误出现“承接上一章”。
3. 后续章节是否能正确结合前文，而不是跑到旧测试内容。
4. 人物性别、身份、关系是否前后一致。

### 3. 生成正文

1. 设置目标字数，例如 1000 或 1500。
2. 生成正文。
3. 等待生成完成后，检查实际字数和结尾完整性。

重点观察：

1. 实际字数是否明显超过目标。
2. 生成结束后内容是否被截断。
3. 页面等待完成时提示是否清楚。
4. 章节显示是否和当前正在生成的章节一致。

### 4. 一致性审稿

1. 正文生成完成后，点击一致性审稿。
2. 查看审稿报告和修改建议。
3. 测试一键套用建议。

重点观察：

1. 第一次点击审稿是否能正常返回结果。
2. 审稿完成后页面是否一直卡在刷新状态。
3. 建议是否完整，替换后上下文是否连贯。
4. 哪些建议自动替换成功，哪些需要手动处理，页面提示是否清楚。

### 5. 章节管理

1. 生成多章后测试分页。
2. 测试跳转到指定章节。
3. 删除某一章后重新生成。

重点观察：

1. 分页是否按预期显示。
2. 章节跳转是否准确。
3. 删除中间章节后，后续生成是否还能正确参考前面章节。

## 需要反馈的问题

遇到下面情况都建议反馈：

1. 无法安装、无法启动、白屏或卡死。
2. 授权码无法激活，或激活后状态异常。
3. AI 设置保存失败，或测试连接失败。
4. 生成内容明显跑题、断章、截断、性别错乱。
5. 删除章节后重新生成仍引用旧内容。
6. 审稿报告没有结果、重复点击才有结果、建议替换不完整。
7. 字数控制严重不准。
8. 页面错位、按钮无效、复制无效、深色模式颜色异常。
9. 首页、项目中心、创作工作台数据不同步。

## 反馈格式

建议按下面格式反馈，方便快速定位：

```text
问题标题：
系统版本：
客户端版本：
授权码后四位：
发生页面：
操作步骤：
期望结果：
实际结果：
是否稳定复现：
截图或录屏：
```

示例：

```text
问题标题：删除第一章后重新生成仍然承接上一章
系统版本：Windows 11
客户端版本：1.0.0
授权码后四位：1234
发生页面：创作工作台
操作步骤：生成第一章任务卡 -> 删除 -> 重新生成第一章任务卡
期望结果：第一章从开篇开始写，不引用已删除内容
实际结果：任务卡出现“承接上一章”，并引用已删除内容
是否稳定复现：是
截图或录屏：已附
```

## 已知限制

1. 当前内测包暂未做正式 Developer ID / Windows Authenticode 代码签名。
2. macOS 内测包使用 ad-hoc 签名，需要用户首次打开时手动允许。
3. 当前主要验证 Windows x64 客户端。
4. 自动静默更新暂未开放，但客户端已支持在“设置 -> 检查更新”里查看新版并手动下载安装。
5. 用户需要自行配置 AI 服务商信息。
6. 生成质量会受到模型能力、提示词、题材信息完整度影响。

## 发布前自检

每次发内测包前，建议在项目目录执行：

```bash
npm run desktop:dist:release
```

如果只发 Windows 包，也可以只执行：

```bash
npm run desktop:dist:win
npm run release:check
```

发布新版时，还需要在授权中心部署环境更新版本信息：

```text
APP_LATEST_VERSION
APP_LATEST_RELEASE_NOTES
APP_UPDATE_DOWNLOAD_WIN_URL
APP_UPDATE_DOWNLOAD_MAC_ARM64_URL
```

如果使用 `npm run desktop:publish` 上传安装包，则不需要手动更新这些下载链接环境变量。上传脚本会在服务器 `public/downloads/manifest.json` 里写入最新版本、安装包地址和文件大小。

更新完成后，内测用户可以：

1. 打开服务器 `/download` 页面下载新版。
2. 在客户端右上角“设置 -> 检查更新”查看新版本。

如果要发 macOS 包，按目标用户芯片执行：

```bash
npm run desktop:dist:mac:arm64
npm run release:check:mac:arm64

npm run desktop:dist:mac:x64
npm run release:check:mac:x64
```

自检通过后，优先分发这个文件：

```text
release/AI网文写作助手-Setup-1.0.0-x64.exe
```

不要把 `win-unpacked` 目录直接发给普通内测用户，也不要混发历史 arm64 安装包。
